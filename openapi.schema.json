{
  "openapi": "3.0.0",
  "info": {
    "title": "Learning Resources",
    "version": "1.0.0",
    "description": "// Generated by index.ts - DO NOT EDIT THIS FILE DIRECTLY"
  },
  "components": {
    "schemas": {
      "EntityTag": {
        "type": "string",
        "enum": [
          "clojure",
          "cpp",
          "general",
          "git",
          "haskell",
          "java",
          "kotlin",
          "php",
          "programming-language-design",
          "python",
          "rust",
          "sql"
        ]
      },
      "PaidPricing": {
        "type": "object",
        "properties": {
          "model": {
            "type": "string",
            "enum": [
              "Subscription",
              "One Time"
            ],
            "description": "The Paid Pricing Model of this resource. 'Subscription' means the resource is paid on a recurring basis (e.g. monthly or yearly), while 'One Time' means the resource is paid with a single upfront payment. If the price varies or is not fixed, provide a close approximation. Note that the subscription renewal cycle is not specified, so if the price has different renewal cycles, provide the most common or default one (usually monthly)."
          },
          "amount": {
            "type": "number",
            "minimum": 0,
            "exclusiveMinimum": true,
            "description": "The price of this resource, in US Dollars."
          }
        },
        "required": [
          "model",
          "amount"
        ],
        "additionalProperties": false
      },
      "FreePricing": {
        "type": "object",
        "properties": {
          "model": {
            "type": "string",
            "enum": [
              "Free",
              "Freemium"
            ],
            "description": "The Free(mium) Pricing Model of this resource. 'Free' should be used for resources where 100% (or close) of the content is free. 'Freemium' describes a pricing model where the core content is available for free, but features paid extensions. If the resource has a freemium model but the free portion is very limited, consider using 'Paid' instead and providing an estimated price for the full version. "
          }
        },
        "required": [
          "model"
        ],
        "additionalProperties": false
      },
      "Pricing": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/FreePricing"
          },
          {
            "$ref": "#/components/schemas/PaidPricing"
          }
        ],
        "discriminator": {
          "propertyName": "model",
          "mapping": {
            "Free": "#/components/schemas/FreePricing",
            "Freemium": "#/components/schemas/FreePricing",
            "Subscription": "#/components/schemas/PaidPricing",
            "One Time": "#/components/schemas/PaidPricing"
          }
        },
        "description": "Details about the cost of the resource."
      },
      "LanguageDomain": {
        "type": "string",
        "enum": [
          "Web Development",
          "Data Science",
          "Mobile Development",
          "Game Development",
          "Systems Programming",
          "Scripting",
          "General Purpose",
          "DevOps"
        ],
        "description": "A domain that a programming language may be used in."
      },
      "ProgrammingParadigm": {
        "type": "string",
        "enum": [
          "Object-Oriented Programming",
          "Functional Programming",
          "Procedural Programming",
          "Logic Programming"
        ],
        "description": "A programming paradigm."
      },
      "ResourceCategory": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/CategoryLanguage"
          },
          {
            "$ref": "#/components/schemas/CategoryPlatform"
          },
          {
            "$ref": "#/components/schemas/CategoryTool"
          }
        ],
        "discriminator": {
          "propertyName": "type",
          "mapping": {
            "Language": "#/components/schemas/CategoryLanguage",
            "Platform": "#/components/schemas/CategoryPlatform",
            "Tool": "#/components/schemas/CategoryTool"
          }
        }
      },
      "CategoryLanguage": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "Language"
            ]
          },
          "paradigms": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProgrammingParadigm"
            },
            "description": "The programming paradigms that this language focuses on, e.g. 'Object-Oriented Programming', 'Functional Programming', 'Procedural Programming', etc."
          }
        },
        "required": [
          "type",
          "paradigms"
        ]
      },
      "CategoryPlatform": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "Platform"
            ]
          }
        },
        "required": [
          "type"
        ],
        "description": "A platform used to learn programming, which may teach a variety of languages and concepts."
      },
      "CategoryTool": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "Tool"
            ]
          }
        },
        "required": [
          "type"
        ]
      },
      "ResourceType": {
        "type": "string",
        "enum": [
          "Video",
          "Article",
          "Interactive Tutorial",
          "Book",
          "Course"
        ],
        "description": "The type of the resource"
      },
      "Resource": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The official name of the resource"
          },
          "description": {
            "type": "string",
            "maxLength": 256,
            "description": "A brief description of the resource"
          },
          "url": {
            "type": "string",
            "format": "uri",
            "description": "URL to the resource"
          },
          "type": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ResourceType"
            },
            "minItems": 1,
            "description": "The type(s) of the resource, e.g. 'Video', 'Book', 'Course', etc."
          },
          "teaches": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EntityTag"
            },
            "minItems": 1,
            "description": "The topics that this resource teaches."
          },
          "pricing": {
            "$ref": "#/components/schemas/Pricing"
          },
          "pros": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Array of pros for using the resource, e.g. 'explains difficult concepts with good analogies'"
          },
          "cons": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Array of cons for using the resource, e.g. 'only teaches the basics rather than more advanced concepts'"
          }
        },
        "required": [
          "name",
          "url",
          "type",
          "teaches",
          "pricing"
        ],
        "additionalProperties": false
      },
      "Meta": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The name of the language, tool, etc being described by this metadata."
          },
          "description": {
            "type": "string",
            "description": "A brief description of the language, tool, etc being described by this metadata."
          },
          "emoji": {
            "type": "string",
            "description": "A Unicode emoji glyph to represent the entity, if applicable. If there is no suitable (Unicode) emoji, omit this field. Consumers may choose to ignore this field, or replace it with a custom image."
          },
          "domains": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LanguageDomain"
            },
            "description": "The domain(s) that the entity is commonly used in, or best suited for."
          },
          "category": {
            "$ref": "#/components/schemas/ResourceCategory"
          }
        },
        "required": [
          "name",
          "description",
          "domains",
          "category"
        ],
        "additionalProperties": false
      },
      "Database": {
        "type": "object",
        "properties": {
          "metadata": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CompiledMeta"
            },
            "description": "List of all entities in the system"
          },
          "resources": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Resource"
            },
            "description": "List of all learning resources"
          }
        },
        "required": [
          "metadata",
          "resources"
        ],
        "additionalProperties": false
      },
      "CompiledMeta": {
        "allOf": [
          {
            "$ref": "#/components/schemas/Meta"
          },
          {
            "type": "object",
            "properties": {
              "id": {
                "$ref": "#/components/schemas/EntityTag"
              }
            },
            "required": [
              "id"
            ],
            "additionalProperties": false
          }
        ],
        "id": "CompiledMeta"
      }
    },
    "parameters": {}
  },
  "paths": {
    "/resources": {
      "get": {
        "summary": "Get all learning resources",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Database"
                }
              }
            }
          }
        }
      }
    }
  }
}
